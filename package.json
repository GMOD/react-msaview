{
  "private": true,
  "workspaces": [
    "lib",
    "app"
  ],
  "scripts": {
    "lib": "cd lib; npm run start",
    "app": "cd app; npm run start",
    "start": "concurrently \"yarn lib\"  \"echo Compiling lib... && wait-on http://localhost:9000/dist/react-msaview.cjs.development.js && echo TSDX finished && yarn app\""
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "serve": "^11.3.2",
    "wait-on": "^5.3.0"
  }
}
