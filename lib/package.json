{
  "name": "react-msaview",
  "author": "Colin",
  "version": "3.1.5",
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "dist",
    "bundle",
    "src"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "prepublishOnly": "node output-version.js > src/version.ts && git add -A src && git commit -m '[skip ci] Bump version.ts'",
    "build:esm": "tsc",
    "build:bundle": "rollup -c --bundleConfigAsCjs",
    "build": "npm run build:esm && npm run build:bundle",
    "prepack": "npm run build",
    "postversion": "git push --follow-tags",
    "prestatedocs": "yarn tsc --build docgen/tsconfig.json",
    "statedocs": "rm -rf apidocs && mkdir apidocs && node docgen/dist/generateStateModelDocs.js"
  },
  "peerDependencies": {
    "@jbrowse/core": ">=2.0.0",
    "@mui/icons-material": ">=5.0.0",
    "@mui/material": ">=5.0.0",
    "mobx": ">=6.0.0",
    "mobx-react": ">=7.0.0",
    "mobx-state-tree": ">=5.0.0",
    "react": ">=16.8.0",
    "react-dom": ">=16.8.0"
  },
  "dependencies": {
    "canvas2svg": "^1.0.16",
    "clustal-js": "^2.0.0",
    "colord": "^2.9.3",
    "copy-to-clipboard": "^3.3.1",
    "d3-array": "^3.2.3",
    "d3-hierarchy": "^3.1.2",
    "file-saver": "^2.0.5",
    "normalize-wheel": "^1.0.1",
    "rbush": "^3.0.1",
    "stockholm-js": "^1.0.10",
    "svgcanvas": "^2.5.0",
    "tss-react": "^4.8.3"
  }
}
